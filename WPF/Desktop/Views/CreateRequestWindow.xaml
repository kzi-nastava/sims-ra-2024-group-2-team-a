<Window x:Class="BookingApp.WPF.Desktop.Views.CreateRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.Desktop.Views"
        mc:Ignorable="d"
        Title="CreateRequestWindow" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Background" Value="#88C0D0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="1"
                    Margin="5"
                    BorderBrush="Black"
                    BorderThickness="1">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" 
                            BorderBrush="Black" 
                            BorderThickness="1" 
                            Background="#2E3440">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock FontSize="24"
                                       Foreground="White"
                                       Grid.Row="0"
                                       Grid.ColumnSpan="3"
                                       Margin="5"
                               >Personal information</TextBlock>
                            <StackPanel Grid.Row="1"
                                        Margin="5">
                                    <TextBlock FontSize="18"
                                               Foreground="White">Name</TextBlock>
                                    <TextBox></TextBox>
                                </StackPanel>

                            <StackPanel Grid.Row="1"
                                        Grid.Column="1"
                                        Margin="5">
                                <TextBlock FontSize="18"
                                           Foreground="White">Surname</TextBlock>
                                <TextBox></TextBox>
                            </StackPanel>
                            <StackPanel Grid.Row="1"
                                        Grid.Column="2"
                                        Margin="5">
                                <TextBlock FontSize="18"
                                           Foreground="White">Age</TextBlock>
                                <TextBox></TextBox>
                            </StackPanel>

                            
                            <Button x:Name="AddTouristButton"
                                    Style="{StaticResource ButtonStyle}"
                                    IsEnabled="{Binding IsTouristButtonEnabled}"
                                    Grid.RowSpan="2"
                                    Grid.Column="3"
                            >Add</Button>
                        </Grid>

                    </Border>
                    <Border Grid.Row="1" 
        BorderBrush="Black" 
        BorderThickness="1" 
        Background="#2E3440">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock FontSize="24"
                   Foreground="White"
                   Grid.Row="0"
                   Grid.ColumnSpan="3"
                   Margin="5"
           >Passenger information</TextBlock>
                            <StackPanel Grid.Row="1"
                    Margin="5">
                                <TextBlock FontSize="18"
                           Foreground="White">Name</TextBlock>
                                <TextBox></TextBox>
                            </StackPanel>

                            <StackPanel Grid.Row="1"
                    Grid.Column="1"
                    Margin="5">
                                <TextBlock FontSize="18"
                       Foreground="White">Surname</TextBlock>
                                <TextBox></TextBox>
                            </StackPanel>
                            <StackPanel Grid.Row="1"
                    Grid.Column="2"
                    Margin="5">
                                <TextBlock FontSize="18"
                       Foreground="White">Age</TextBlock>
                                <TextBox></TextBox>
                            </StackPanel>


                            <Button
                Style="{StaticResource ButtonStyle}"
                IsEnabled="{Binding IsTouristButtonEnabled}"
                Grid.RowSpan="2"
                Grid.Column="3"
        >Add</Button>
                        </Grid>

                    </Border>

                    
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <DataGrid ItemsSource="{Binding Passengers, UpdateSourceTrigger=PropertyChanged}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  Grid.Column="0"
                                  Width="500"
                                  Height="330">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" 
                                Binding="{Binding Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Surname"
                                Binding="{Binding Surname, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                                <DataGridTextColumn Header="Age"
                                Binding="{Binding Age, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                                <DataGridTemplateColumn>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="RemovePassengerButton"
                                            Content="Remove"
                                            Click="RemovePassengerButton_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>

                        </DataGrid>
                        <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">
                            <TextBlock x:Name="UseCouponsLink"
                               FontSize="20"
                               Margin="5, 0, 0, 0"
                               MouseLeftButtonDown="UseCouponsLink_Click"
                               >
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Use coupons?"></Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsVoucherSelected}" Value="True">
                                                <Setter Property="Text"
                                                Value="Remove coupon"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Button x:Name="ConfirmReservationButton"
                            Style="{StaticResource ButtonStyle}"
                            Click="ConfirmReservationButton_Click"
                            IsEnabled="{Binding IsConfirmationButtonEnabled}"
                            >Book</Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

        </Grid>
        
        <Button Grid.Row="1"
                Content="Create Request"
                Style="{StaticResource ButtonStyle}"/>
    </Grid>
</Window>
